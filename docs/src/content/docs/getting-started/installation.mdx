---
title: Installation
description: Install the PrismaLens CLI from npm and verify your setup with pl doctor.
---

The `prismalens` CLI (alias `pl`) investigates a firing alert straight from
your terminal — no account, no server, bring your own model provider key.

## Prerequisites

- Node.js 22+
- A model provider credential (BYO-key — see
  [providers & harnesses](/cli/providers/) for what's supported)

## Try it without installing

```bash
npx prismalens doctor
```

`doctor` checks that a harness binary and an LLM credential are on your
machine before you run anything.

## Install it

```bash
npm install -g prismalens
```

This gives you both the `prismalens` and `pl` commands.

## Next steps

- Run your first investigation: [CLI overview](/cli/)
- Full flag reference: [commands](/cli/commands/)
- Set up a provider: [providers & harnesses](/cli/providers/)
- Scaffold a config: [configuration](/cli/configuration/)

:::note[Looking for the self-hosted server?]
The PrismaLens server — web UI, webhook alert intake, and team features on
top of the same engine — is in development and not released yet. The CLI is
the released surface today.
:::
